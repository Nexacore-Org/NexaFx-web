/**
 * @deprecated All transaction types and data are now sourced from lib/api/transactions.ts.
 * This file is kept temporarily and can be deleted once confirmed no other imports remain.
 */

export type { TransactionStatus, TransactionType, Transaction } from "@/lib/api/transactions";

export const mockTransactions: never[] = [];
